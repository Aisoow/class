# 1-7 人類基因組檢測 - 完整教學

## 📋 題目概述

本題目要求檢測一系列 DNA 序列中，有多少條序列符合「人類基因組」的標準。判斷標準是：序列中的每個字元都必須出現在允許的字元集合中。

---

## 🎯 解題思路

### 核心概念
1. **字串查找**：使用 `string::find()` 檢查字元是否存在
2. **向量遍歷**：遍歷所有 DNA 序列進行檢測
3. **布林標記**：使用 flag 標記序列是否符合條件

### 演算法流程
```
1. 讀取允許的字元集合（allowed）
2. 讀取 DNA 序列數量和所有序列
3. 對每個序列：
   a. 假設該序列符合條件（is_human = true）
   b. 檢查序列中的每個字元
   c. 如果發現不允許的字元，標記為不符合並跳出
   d. 如果通過所有檢查，計數器加一
4. 輸出符合條件的序列總數
```

---

## 💻 程式碼詳解

### 完整程式碼
```cpp
#include <iostream>
#include <string>
#include <vector>
using namespace std;

class Solution
{
public:
    int detectHumanGenome(string allowed, vector<string> &datas)
    {
        int count = 0;
        for (string data : datas)
        {
            bool is_human = 1;
            for (char c : data)
            {
                if (allowed.find(c) == string::npos)
                {
                    is_human = 0;
                    break;
                }
            }
            if (is_human)
            {
                count++;
            }
        }
        return count;
    }
};

int main(int argc, char const *argv[])
{
    string allowed, data;
    vector<string> datas;
    int num, ans = 0;
    cin >> allowed >> num;

    for (int i = 0; i < num; i++)
    {
        cin >> data;
        datas.push_back(data);
    }

    Solution sol;
    ans = sol.detectHumanGenome(allowed, datas);
    cout << ans;

    return 0;
}
```

---

## 🔍 關鍵知識點

### 1. `string::find()` 函式
```cpp
size_t find(char c);
```
- **功能**：在字串中查找字元
- **回傳值**：
  - 找到：返回字元的位置（從 0 開始）
  - 未找到：返回 `string::npos`

### 2. `string::npos` 常數
```cpp
if (allowed.find(c) == string::npos)
```
- **意義**：表示「未找到」的特殊值
- **值**：通常為 `-1`（在 `size_t` 中是最大值）
- **常見錯誤**：❌ 使用 `NULL` 而非 `string::npos`

### 3. Range-based for loop（範圍 for 迴圈）
```cpp
for (string data : datas)  // 遍歷 vector
for (char c : data)        // 遍歷 string
```

---

## 📊 範例執行

### 輸入範例 1
```
ATCG 3
ATCGATCG
XATCG
ATCG
```

### 執行過程
1. `allowed = "ATCG"`, `num = 3`
2. 檢測序列：
   - `"ATCGATCG"`: 所有字元都在 ATCG 中 ✅
   - `"XATCG"`: 包含 'X' 不在 ATCG 中 ❌
   - `"ATCG"`: 所有字元都在 ATCG 中 ✅
3. 符合條件的序列數 = 2

### 輸出
```
2
```

---

## ⚠️ 常見錯誤

### 錯誤 1：使用 NULL 而非 string::npos
```cpp
// ❌ 錯誤
if (allowed.find(c) == NULL)

// ✅ 正確
if (allowed.find(c) == string::npos)
```

### 錯誤 2：忘記 break
```cpp
// ❌ 沒有 break，會繼續檢查其他字元
if (allowed.find(c) == string::npos)
{
    is_human = 0;
    // 缺少 break
}

// ✅ 正確
if (allowed.find(c) == string::npos)
{
    is_human = 0;
    break;  // 立即停止檢查
}
```

### 錯誤 3：邏輯錯誤
```cpp
// ❌ 應該是找不到時設為 false
if (allowed.find(c) != string::npos)
    is_human = 0;

// ✅ 正確
if (allowed.find(c) == string::npos)
    is_human = 0;
```

---

## 🚀 時間複雜度分析

- **外層迴圈**：遍歷 n 個序列 → O(n)
- **內層迴圈**：檢查每個序列的 m 個字元 → O(m)
- **find() 操作**：在長度為 k 的字串中查找 → O(k)
- **總時間複雜度**：O(n × m × k)

### 優化建議
可以使用 `unordered_set` 來儲存允許的字元，將查找時間降至 O(1)：
```cpp
unordered_set<char> allowedSet(allowed.begin(), allowed.end());
if (allowedSet.find(c) == allowedSet.end())
```

---

## 🎓 延伸學習

### 相關主題
1. **字串操作**：`find()`, `substr()`, `replace()`
2. **容器操作**：`vector`, `set`, `unordered_set`
3. **演算法**：線性搜尋、雜湊表

### 練習題目
1. 修改程式輸出不符合條件的序列
2. 統計每個序列中不符合的字元數量
3. 使用 `unordered_set` 優化查找效率

---

## 📝 總結

本題考察的核心能力：
- ✅ 字串查找操作
- ✅ 向量遍歷與處理
- ✅ 條件判斷與計數
- ✅ 物件導向程式設計

掌握 `string::find()` 和 `string::npos` 的正確用法是解決本題的關鍵！
