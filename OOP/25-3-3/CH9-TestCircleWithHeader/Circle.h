#ifndef CIRCLE_H
#define CIRCLE_H

class Circle
{
public:
    double radius; // The radius of this circle

    // Constructors
    Circle();
    Circle(double newRadius);

    // Member function
    double getArea();
};

#endif
